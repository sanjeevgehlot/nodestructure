
var path = require('path');

var config = {
	debug: true,
	port: 3000,
	mysql: {
		host: 'localhost',
		username: 'root',
		password: 'root',
		database: 'streettaglocal',
		port : '8889'
	}
}

module.exports = config